// find chat request.
if (find) {
    // find room.
    if (find) {
        // chat code.
        if (chart_create) {
            // create chat.
        } else {
            // chat not create.
        }
    }
    else {
        // room code.
        if (create_room) {

            if (chart_create) {
                // create chat.
            } else {
                // chart not create.
            }
        } else {
        }
    }
}
else {
    // create chat request.
    if(craete)

}